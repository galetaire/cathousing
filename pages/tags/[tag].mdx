---
type: tag
title: Tagged Posts
---

import { useRouter } from 'next/router'

export const TagName = () => {
  const { tag } = useRouter().query
  return tag || null
}

export const GoBack = () => {
  const { meta-back } = useRouter().query
  return <link href="/">
}

# Posts Tagged with “<TagName/>
